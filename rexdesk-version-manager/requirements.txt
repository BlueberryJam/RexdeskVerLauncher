# Product logos: SVG raster (PyMuPDF) + PIL resize; PNG fallbacks in assets/
Pillow>=10.0.0
pymupdf>=1.24.0

# Optional: alternate SVG raster if PyMuPDF unavailable (needs Cairo on Windows)
# cairosvg>=2.7.0
